{
  "name": "Docpine Dev Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": [
    "-v", "/var/run/docker.sock:/var/run/docker.sock"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.go"
      ]
    }
  }
}
